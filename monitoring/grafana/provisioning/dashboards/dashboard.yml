apiVersion: 1

providers:
  - name: "fiap-x-dashboards"
    orgId: 1
    folder: "FIAP X"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
